#A package is an organised container for a set of modules
#it is a directory
#PACKAGES contain a set of related MODULES

#to initiate a PACKAGE, you can simply create a PYTHON PACKAGE

#then to import the module in the package, we  use IMPORT (PACKAGE).(MODULE)
#OR more easily FROM (PACKAGE) IMPORT (MODULE)